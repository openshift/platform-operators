# Adds namespace to all resources.
namespace: openshift-platform-operators

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: platform-operators-

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

# Annotations to add to all resources.
commonAnnotations:
  release.openshift.io/feature-set: "TechPreviewNoUpgrade"
  include.release.openshift.io/self-managed-high-availability: "true"
  include.release.openshift.io/single-node-developer: "true"
  exclude.release.openshift.io/internal-openshift-hosted: "true"

images:
- name: quay.io/brancz/kube-rbac-proxy
  newName: gcr.io/kubebuilder/kube-rbac-proxy
  newTag: v0.8.0
# Edit this image if you would like to use a custom version of PlatformOperators
- name: controller:latest
  newName: quay.io/openshift/origin-cluster-platform-operators-manager
  newTag: "4.12"

bases:
- ../rbac
- ../manager
- ../clusteroperator
# [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
#- ../prometheus

patches:
# Protect the /metrics endpoint by putting it behind auth.
# If you want your controller-manager to expose the /metrics
# endpoint w/o any authn/z, please comment the following line.
- path: manager_auth_proxy_patch.yaml

# Adding the workload pinning annotation to all deployment pod templates.
# This allows the workload pinning webhook to know which pods to modify
# with the specific custom resources for workload pinning to work.
- patch: |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: platform-operators-*
    spec:
      template:
        metadata:
          annotations:
            target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}' 
  target:
    kind: Deployment

# Adding the workload pinning annotation to the namespace.
# In order for any workload pinned pod to run in this namespace, the below
# annotation is required.
- patch: |-
    apiVersion: v1
    kind: Namespace
    metadata:
      annotations:
        workload.openshift.io/allowed: "management"
      name: openshift-*
  target:
    kind: Namespace
# Mount the controller config file for loading manager configurations
# through a ComponentConfig type
#- manager_config_patch.yaml
